import FeedbackService, {
  submitFeedback,
  getFeedback,
  deleteFeedback,
  getFeedbackStats,
  getItems,
  createItem
} from './FeedbackService';

export {
  submitFeedback,
  getFeedback,
  deleteFeedback,
  getFeedbackStats,
  getItems,
  createItem
};

export default FeedbackService;
